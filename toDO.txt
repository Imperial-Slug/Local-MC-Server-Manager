check if path is minecraft server function
check if path is already part of the server_array function
Display new "server added" screen on success
Ask whether to return to main menu or add another server.
Handle return and add options

Error handling idea for finding servers : ask if its' possible that the name of the server file is different than this program expects and to enter the custom name and save it or exit back to the menu or q to quit the program
split the program into separate sh files for each option

Handle events surrounding download of jars
make program move the jar to the new server's directory.
